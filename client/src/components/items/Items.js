import React, { useState } from 'react';

// this will hold all of the players items in a list in there character sheet

const Items = props => {

  const [ items, setItems ] = useState([])

}

export default Items;